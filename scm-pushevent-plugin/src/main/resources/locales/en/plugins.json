{
  "scm-pushevent-plugin": {
    "settings": {
      "navLink": "Push Event Plugin",
      "title": "Push Event Plugin Config"
    },
    "config": {
      "form": {
        "active": "active",
        "activeHelpText": "controls whether data is sent or not",
        "endpointUrl": "url to endpoint",
        "endpointUrlHelpText": "url to which push data is sent",
        "token": "Auth token",
        "tokenHelpText": "JWT Auth token. It can be generated by an admin in the events.clougogu.com project."
      }
    }
  }
}
